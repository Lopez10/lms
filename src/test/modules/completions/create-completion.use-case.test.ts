describe('create completion use case', () => {
	it(`
		GIVEN there is a lesson id and a user id
		WHEN I create a completion
		THEN the completion is created successfully
	`, () => {
		// GIVEN
		// WHEN
		// THEN
	});
});
